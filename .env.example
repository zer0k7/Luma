# Luma Environment Configuration
# Luma is designed to work offline without telemetry or external network services.
# Any local runtime configuration overrides can be placed here.

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LUMA_LOG_LEVEL=INFO
